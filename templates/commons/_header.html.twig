<header>
    <nav>
        <ul>
            <li><a href="{{ path('app_game_index')}}">Home</a></li>
            <li> <a href="{{ path('app_user', {'id': app.user.id}) }}">Profile</a></li>
            {% if app.user %}
            <li><a href="{{ path('app_logout')}}">Logout</a></li>
            {% else %}
            <li><a href="{{ path('app_login')}}">Sign In</a></li>
            {% endif %}
        </ul>
    </nav>
</header>