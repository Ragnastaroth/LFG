{% extends 'base.html.twig' %}

{% block title %}Edit {{app.user.username}}'s profile{% endblock %}

{% block body %}
    <h1>Edit Profile</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update your profile'}) }}

    <a href="{{ path('app_user', {'id': app.user.id}) }}">back to profile</a>

{% endblock %}